machine:
  java:
    version: oraclejdk8

test:
  override:
    - sbt coverage test
    - sbt coverageReport
    - sbt coverageAggregate
    # - sbt coveralls
  post:
    - cp auth-*/target/test-reports/* $CIRCLE_TEST_REPORTS
